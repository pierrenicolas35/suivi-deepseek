#!/usr/bin/env bash
# Recalcule docs/data/suivi.json à partir de la base de sessions goose.
set -euo pipefail
script="$(readlink -f "${BASH_SOURCE[0]}")"
racine="$(cd "$(dirname "$script")/.." && pwd)"
exec python3 "$racine/outils/collecter.py" "$@"
